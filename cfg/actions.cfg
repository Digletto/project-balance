// $Action to define the way to calculate an actions outcome. Ended with >@ 
// All inputs separated with new line

$attack (Actor pc, int level)
	int roll = ThreadLocalRandom.current().nextInt(0, 20 + 1);
	if ()
>$

$defend
>$
